server:
    port: 9993

spring:
  application:
    name: TAX-SERVICE
  cloud:
    config: 
       enabled: false 
management:
  endpoints:
    web:
      #base-path: /actuator
      exposure:
        include: health,keepalive             